const SmallSectionTitle = ({ title }: any) => {
  return (
    <div className="small-section-title-container">
      <div className="small-section-title">{title}</div>
    </div>
  );
};

export default SmallSectionTitle;
